import { Answers } from 'inquirer';
import { BaseStep } from './BaseStep';
export declare class PromptForParameters extends BaseStep {
    emit(answers: Answers): Promise<Answers>;
    private getFullUrl(answers, organizationSlug?, projectSlug?);
    private shouldAsk(answers, configKey, preHook?);
    private validateAuthToken(input);
    private validateSlug(input);
    private validateDSN(input);
}
