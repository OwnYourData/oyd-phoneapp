import { Answers } from 'inquirer';
import { Args } from '../../Constants';
import { SentryCli } from '../../Helper/SentryCli';
import { MobileProject } from './MobileProject';
export declare class ReactNative extends MobileProject {
    protected argv: Args;
    protected answers: Answers;
    protected sentryCli: SentryCli;
    constructor(argv: Args);
    emit(answers: Answers): Promise<Answers>;
    uninstall(answers: Answers): Promise<Answers>;
    protected shouldConfigurePlatform(platform: string): Promise<boolean>;
    private addSentryProperties(platform, properties);
    private patchJs(contents, filename, answers, platform?);
    private patchBuildGradle(contents);
    private unpatchBuildGradle(contents);
    private patchExistingXcodeBuildScripts(buildScripts);
    private addNewXcodeBuildPhaseForSymbols(buildScripts, proj);
    private addZLibToXcode(proj);
    private patchAppDelegate(contents);
    private patchXcodeProj(contents, filename);
    private unpatchAppDelegate(contents);
    private unpatchXcodeBuildScripts(proj);
    private unpatchXcodeProj(contents, filename);
}
